# Database Configuration
# Use the same DATABASE_URL as the main backend
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# MCP User ID
# The User ID to impersonate when performing operations (required for MCP protocol)
MCP_USER_ID=default-mcp-user
